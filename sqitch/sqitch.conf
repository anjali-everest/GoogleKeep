[core]
	engine = pg
	# plan_file = sqitch.plan
	# top_dir = .
# [engine "pg"]
	# target = db:pg:
	# registry = sqitch
	# client = psql
[target "googlekeep"]
	uri = db:pg://postgres:postgres@localhost:5432/postgres
[engine "pg"]
	target = db:pg://postgres:postgres@localhost:5432/postgres
